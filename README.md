# SharedVideo Backend

This is the backend service for SharedVideo, a synchronized video watch party app.

## Endpoints

- `POST /upload`: Upload a new video
- `GET /status`: Get current video status
- `POST /ping`: Notify server viewer is active
- `POST /end`: Mark video as ended
- `GET /weather`: Get current weather and time of day